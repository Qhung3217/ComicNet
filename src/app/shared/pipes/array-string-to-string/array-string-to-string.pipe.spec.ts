import { ArrayStringToStringPipe } from './array-string-to-string.pipe';

describe('ArrayStringToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayStringToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
