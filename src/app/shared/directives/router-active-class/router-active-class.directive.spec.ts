import { RouterActiveClassDirective } from './router-active-class.directive';

describe('RouterActiveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new RouterActiveClassDirective();
    expect(directive).toBeTruthy();
  });
});
