import { ClickRemoveClassDirective } from './click-remove-class.directive';

describe('ClickRemoveClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickRemoveClassDirective();
    expect(directive).toBeTruthy();
  });
});
