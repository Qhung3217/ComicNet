import { ImageLoadFailedDirective } from './image-load-failed.directive';

describe('ImageLoadFailedDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageLoadFailedDirective();
    expect(directive).toBeTruthy();
  });
});
